# Business-Card-OCR
A Flask-based OCR Document Scanner that uses OpenCV to detect document edges, scan and correct perspective, then extract text using OCR. It converts photos into clean digital documents with bounding boxes for recognized text, making document digitization fast, accurate, and efficient.
